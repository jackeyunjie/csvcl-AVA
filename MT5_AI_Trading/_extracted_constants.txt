# Decompiled from __pycache__/hermass_state_ops.cpython-312.pyc
# Python version: 3.12

# Constants found in bytecode:
# Const 0:
#   
#               SELECT COUNT(*), MIN(timestamp), MAX(timestamp)
#               FROM h1_state_snapshot
#               WHERE symbol = ?
#               
# Const 1:
#   
#               SELECT COUNT(*), MIN(timestamp), MAX(timestamp)
#               FROM m15_state_snapshot
#               WHERE symbol = ?
#               
# Const 2:
#   
#               SELECT COUNT(*), MIN(date), MAX(date)
#               FROM state_snapshots
#               WHERE symbol = ?
#               
# Const 3:
#   
#           SELECT COUNT(*), COUNT(DISTINCT symbol), MIN(date), MAX(date)
#           FROM state_snapshots
#           
# Const 4:
#   
#           SELECT COUNT(*), COUNT(DISTINCT symbol), MIN(timestamp), MAX(timestamp)
#           FROM h1_state_snapshots
#           
# Const 5:
#   
#           SELECT timestamp, d1_hex, h1_hex, d1_duration, h1_duration
#           FROM h1_state_snapshot
#           WHERE symbol = ?
#           ORDER BY timestamp DESC
#           LIMIT 1
#           
# Const 6:
#   
#           SELECT timestamp, d1_hex, h1_hex, m15_hex,
#                  d1_duration, h1_duration, m15_duration,
#                  sr_breakout, breakout_direction, breakout_tf
#           FROM m15_state_snapshot
#           WHERE symbol = ?
#           ORDER BY timestamp DESC
#           LIMIT 1
#           
# Const 7:
#   
#           SELECT date, mn1_hex, w1_hex, d1_hex, mn1_score, w1_score, d1_score, ef_count
#           FROM state_snapshots
#           WHERE symbol = ?
#           ORDER BY date DESC
#           LIMIT 1
#           
# Const 8:
#   
#   
# Const 9:
#   
#   
# Const 10:
#   
#   
# Const 11:
#   
#   